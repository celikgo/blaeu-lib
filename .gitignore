# Dependencies
node_modules/
.npm

# Build output
dist/
.tsbuild/
*.tsbuildinfo

# The generated docs site. Its source is the markdown in the tree; the workflow
# rebuilds it on every push, so a committed copy could only ever be a stale one.
site/

# Test / coverage
coverage/

# Caches
.cache/
.eslintcache
.vite/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env.*
!.env.example

# Editor / IDE (JetBrains shared project files are tracked; only per-user state is ignored)
.idea/workspace.xml
.idea/tasks.xml

# OS
.DS_Store
Thumbs.db

# Claude Code local settings (personal, per-machine)
.claude/settings.local.json

# Stryker mutation reports and its sandboxes
reports/
.stryker-tmp/
